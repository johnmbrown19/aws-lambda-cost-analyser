# aws-lambda-cost-analyser

Small TypeScript utility for detecting simple AWS service cost anomalies.

## Why
Built as a lightweight infra-focused TypeScript project to demonstrate:
- typed data modelling
- async workflows
- basic anomaly detection logic
- CI readiness with GitHub Actions

## Run
```bash
npm install
npm run start
